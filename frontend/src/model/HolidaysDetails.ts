class HolidaysDetails {
    constructor(public holidayID: number, public place: string, public description:string, public startDate: Date , public endDate: Date, public imageName: string, public price: number) {}
}

export default HolidaysDetails;